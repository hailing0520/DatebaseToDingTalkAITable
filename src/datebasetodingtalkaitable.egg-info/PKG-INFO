Metadata-Version: 2.4
Name: datebasetodingtalkaitable
Version: 0.1.0
Summary: SqlServer 数据同步到钉钉 AI 表格
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: flask>=3.0.0
Requires-Dist: requests>=2.31.0
Requires-Dist: pyodbc>=5.0.0

# 数据库同步钉钉 AI 表格

将 SqlServer 查询结果通过钉钉开放平台接口全量同步到钉钉 AI 表格。

## 功能

- 配置 SqlServer 连接并测试连接
- 输入自定义 SQL，预览结果后选择同步
- 使用 AppKey/AppSecret 获取 Token，选择 AI 表格与数据表，获取字段列表
- 配置数据库列与 AI 表格字段的映射，执行全量同步（必填字段无值时报错）

## 环境

- Python >= 3.11
- SqlServer 需本机已安装 ODBC Driver（如 “ODBC Driver 17 for SQL Server”）

## 安装与运行

本项目可用 **uv** 或 **pip** 安装依赖。

**方式一：使用 uv（推荐，会生成锁文件）**

```bash
# 安装 uv：https://docs.astral.sh/uv/
uv sync

# 启动本地服务
uv run python main.py
```

**方式二：使用 pip**

```bash
pip install flask requests pyodbc

python main.py
```

浏览器打开 http://127.0.0.1:5000 使用操作页面。

## 钉钉侧说明

- 使用**企业内部应用**的 AppKey、AppSecret（获取 Token 文档：<https://open.dingtalk.com/document/development/obtain-the-access-token-of-an-internal-app>）。
- AI 表格 ID 从钉钉 AI 表格的 URL 或分享/设置中获取。
- 若接口路径与当前实现不一致，可在 `src/datebasetodingtalkaitable/config.py` 和 `dingtalk_client.py` 中修改 `DINGTALK_API_BASE` 及请求 path。

## 项目结构

- `main.py`：启动入口
- `src/datebasetodingtalkaitable/`：核心逻辑
  - `app.py`：Flask 路由与 API
  - `dingtalk_client.py`：钉钉 Token、数据表、字段、插入记录
  - `db_client.py`：SqlServer 连接与 SQL 执行
  - `record_value.py`：记录值格式转换与必填校验
  - `sync_service.py`：同步流程
  - `static/index.html`：前台操作页
- `docs/需求与实现方案.md`：需求与实现说明
